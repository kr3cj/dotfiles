[[ $- = *i* ]] || return 0
${IS_MACOS} || return 0

# python -m pip install -U

# disable while we trial Claude Code+Cowork Natively
# export ANTHROPIC_BASE_URL=${CUSTOM_WORK_ANTHROPIC_BASE_URL}
# export ANTHROPIC_AUTH_TOKEN=${CUSTOM_WORK_ANTHROPIC_TOKEN}
