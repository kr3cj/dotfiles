[[ $- = *i* ]] ||  0
${IS_MACOS} || return 0

# gce bash completion
# source "${BASE_PATH}/Caskroom/google-cloud-sdk/latest/google-cloud-sdk/completion.bash.inc"

# gce pathing (sourcing path.bash.inc duplicates entries into ${PATH})
# source "${BASE_PATH}/Caskroom/google-cloud-sdk/latest/google-cloud-sdk/path.bash.inc"

# gcloud info
# gcloud help

# this golang debug variable should be in its own golang file but I'm lazy:
# export GODEBUG=netdns=9

export GEMINI_API_KEY="${CUSTOM_WORK_GEMINI_API_KEY}"
