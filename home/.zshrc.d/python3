[[ $- = *i* ]] || return 0
${IS_MACOS} || return 0

# Setting PATH to use Homebrew Python 3 (instead of system python3)
# TODO: platform specific pathing
alias pip="pip3"
alias python="python3"

# pathadd $(asdf where python)/bin # just do "asdf reshim python" instead
