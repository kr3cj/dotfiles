[[ $- = *i* ]] || return
${IS_MACOS} || return

export GOPATH=${HOME}/.asdf/shims/go
# GOV=$(asdf current golang | sed  's/ (set by .*)//g')
# export GOROOT=/usr/local/opt/go/libexec
# export GOROOT=${HOME}/.asdf/installs/golang/${GOV//[[:space:]]/}/go/
# TODO: GOBIN?

pathadd ${GOPATH}/bin
pathadd ${GOROOT}/bin
