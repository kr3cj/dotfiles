[[ $- = *i* ]] || return
${IS_OSX} || return

# Workaround for OSX incompatibility with commercial routers
[[ $(sysctl -n net.link.ether.inet.arp_unicast_lim) -ne 1 ]] && \
  sudo sysctl -w net.link.ether.inet.arp_unicast_lim=1

MANPATH="/usr/local/opt/coreutils/libexec/gnuman:/usr/local/opt/findutils/share/man:${MANPATH-/usr/share/man}"
MANPATH="/usr/local/opt/gnu-sed/libexec/gnuman:${MANPATH}"
MANPATH="/usr/local/opt/gnu-tar/libexec/gnuman:${MANPATH}"
export MANPATH

# open desktop apps
# pgrep -qf "Slack.app" || open --background -a Slack # this always opens in foreground :(
# pgrep -qf "Spotify.app" || open --background -a Spotify
# pgrep -qf "Backup and Sync.app" || open --background -a "Backup and Sync"
# pgrep -qf "Atom.app" || open --background -a Atom # this always opens in foreground, too
