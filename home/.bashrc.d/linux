[[ $- = *i* ]] || return
${IS_LINUX} || return

[[ -d /build/${CUSTOM_LDAP_NAME}/deploy ]] && cd /build/${CUSTOM_LDAP_NAME}/deploy
if [[ "laphroaig" =~ $(hostname) ]]; then echo "TODO: launch tmux session..."
